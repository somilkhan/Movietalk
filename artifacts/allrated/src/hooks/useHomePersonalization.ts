import { useEffect, useMemo, useState } from "react";
import type { Title } from "@workspace/api-client-react";
import { getAccessToken, getAnonymousDeviceId } from "@/lib/supabase";
export interface WatchProgressItem { id:number; mediaType:"movie"|"tv"; season:number|null; episode:number|null; position_seconds:number; duration_seconds:number; title:string|null; poster_path:string|null; backdrop_path:string|null; }
function toTitle(item:WatchProgressItem):Title{return {id:item.id,mediaType:item.mediaType,title:item.title||"Untitled",posterPath:item.poster_path,backdropPath:item.backdrop_path,overview:"",voteAverage:0,year:null,genreIds:[]} as Title;}
export function useHomePersonalization(){const[continueWatching,setContinueWatching]=useState<WatchProgressItem[]>([]);const[recommendations,setRecommendations]=useState<Title[]>([]);useEffect(()=>{let cancelled=false;const load=async()=>{try{const token=getAccessToken();const did=getAnonymousDeviceId();if(!did&&!token){setContinueWatching([]);setRecommendations([]);return;}const headers:Record<string,string>={};if(token)headers.Authorization=`Bearer ${token}`;if(did)headers["X-Device-Id"]=did;const response=await fetch("/api/progress?list=1",{headers});if(!response.ok)return;const data=await response.json();const items=Array.isArray(data?.items)?data.items as WatchProgressItem[]:[];if(cancelled)return;const active=items.filter(item=>item.duration_seconds<=0||item.position_seconds<item.duration_seconds*.95).slice(0,20);setContinueWatching(active);const sources=items.slice(0,3);const recommendationLists=await Promise.all(sources.map(async item=>{try{const r=await fetch(`/api/catalog/title/similar?mediaType=${item.mediaType}&id=${item.id}`);if(!r.ok)return[] as Title[];const payload=await r.json();return Array.isArray(payload?.results)?payload.results as Title[]:[];}catch{return[] as Title[];}}));if(cancelled)return;const seen=new Set<string>();setRecommendations(recommendationLists.flat().filter(item=>{const key=`${item.mediaType}-${item.id}`;if(seen.has(key)||sources.some(source=>source.mediaType===item.mediaType&&source.id===item.id))return false;seen.add(key);return true;}).slice(0,20));}catch{if(!cancelled){setContinueWatching([]);setRecommendations([]);}}};const onAuth=async()=>{const token=getAccessToken();const did=getAnonymousDeviceId();if(token&&did)try{await fetch('/api/progress',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,"X-Device-Id":did},body:JSON.stringify({merge:true})});}catch{}void load();};void load();window.addEventListener("rabbitrip:progress-updated",load);window.addEventListener("rabbitrip:auth-changed",onAuth);return()=>{cancelled=true;window.removeEventListener("rabbitrip:progress-updated",load);window.removeEventListener("rabbitrip:auth-changed",onAuth);};},[]);return{continueWatching,continueTitles:useMemo(()=>continueWatching.map(toTitle),[continueWatching]),recommendations};}
